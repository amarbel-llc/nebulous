# eng-versioning(7): the single source of truth for this repo's version. The key
# must be <YOUR_REPO>_VERSION (uppercase, `-`→`_`) — matching what eng-versioning
# derives from go.mod / Cargo.toml, or your linters.eng-versioning.key. Rename the
# placeholder below.
export NEBULOUS_VERSION=0.1.0
